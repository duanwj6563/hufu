package com.feo.domain.repository.strategy;

import com.feo.domain.model.strategy.ClassSize;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClassSizeRepository extends JpaRepository<ClassSize,Long> {

}
